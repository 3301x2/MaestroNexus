name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  typecheck-cli:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: maestronexus/package-lock.json
      - run: npm ci
        working-directory: maestronexus
      - run: npx tsc --noEmit
        working-directory: maestronexus

  typecheck-web:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: maestronexus-web/package-lock.json
      - run: npm ci
        working-directory: maestronexus-web
      - run: npm run typecheck
        working-directory: maestronexus-web
