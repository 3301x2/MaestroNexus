name: CI

on:
  pull_request:
    branches: [main]

jobs:
  typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: maestronexus/package-lock.json
      - run: npm ci
        working-directory: maestronexus
      - run: npx tsc --noEmit
        working-directory: maestronexus
